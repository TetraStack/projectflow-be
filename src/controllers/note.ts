import { asyncHandler } from "@/utils/asyncHandler";
import { Request, Response } from "express";

export const getNotes = asyncHandler((req: Request, res: Response) => { })

export const getNoteById = asyncHandler((req: Request, res: Response) => { })

export const createNote = asyncHandler((req: Request, res: Response) => { })

export const updateNote = asyncHandler((req: Request, res: Response) => { })

export const deleteNote = asyncHandler((req: Request, res: Response) => { })
