import { asyncHandler } from "@/utils/asyncHandler";
import { Request, Response } from "express";

export const createSubTask = asyncHandler((req: Request, res: Response) => { })

export const createTask = asyncHandler((req: Request, res: Response) => { })

export const deleteSubTask = asyncHandler((req: Request, res: Response) => { })

export const deleteTask = asyncHandler((req: Request, res: Response) => { })

export const getTaskById = asyncHandler((req: Request, res: Response) => { })

export const getTasks = asyncHandler((req: Request, res: Response) => { })

export const updateSubTask = asyncHandler((req: Request, res: Response) => { })

export const updateTask = asyncHandler((req: Request, res: Response) => { })

